{
  "scripts": {
    "dev": "concurrently -c \"blue, green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "NODE_ENV=production npm start --prefix backend"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
